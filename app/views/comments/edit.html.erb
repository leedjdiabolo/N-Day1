<%= form_for @comments,:url => comment_path(@comments,:pid => params[:pid],:uid => params[:uid]) ,:method => :patch do |f| %>

	<%= f.label :content, "Content" %>
	<%= f.text_area :content %>

	<%= f.hidden_field :post_id ,:value => params[:pid] %>

	<br>
	<%= f.submit "Edit" %>

<% end %> 